export const initialEditorValue =
  "// let's write some broken code 😈" +
  "\n" +
  "not := comp(pair(iden)(unit))(case(injr(unit))(injl(unit)))" +
  "\n" +
  "half-adder := case(drop(pair(injl(unit))(iden)))(drop(pair(iden)(not)))";
